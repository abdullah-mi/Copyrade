{
  "name": "copyrade",
  "private": true,
  "version": "0.0.0",
  "packageManager": "npm@11.19.0",
  "engines": {
    "node": ">=24.0.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace @copyrade/connection && npm run build --workspace @copyrade/protocol && npm run build --workspace @copyrade/signal && npm run build --workspace @copyrade/mobile && npm run build --workspace @copyrade/desktop",
    "check": "npm run lint && npm test && npm run build",
    "lint": "npm run lint --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  }
}
